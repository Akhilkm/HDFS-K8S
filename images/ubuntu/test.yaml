apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
      - name: config-volume
        mountPath: /root/Dockerfile
        subPath: Dockerfile
  volumes:
    - name: config-volume
      configMap:
        name: test
